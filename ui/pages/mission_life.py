"""Mission LiFE tab — placeholder ("coming soon").

Same shape as critical_minerals.py: a tab with no model behind it yet, kept in
the row (and marked in tabs.py's UPCOMING_TABS) rather than hidden, because
"this is planned" is real structure for a reader deciding what the tool covers.
"""


def render():
    return """
    <div class="view" id="view-mission-life">
      <div class="coming-soon-note">Mission LiFE — coming soon</div>
    </div><!-- /view-mission-life -->
"""
